/** @type {import('tailwindcss').Config} */
module.exports = {
    darkMode: 'class',         // use `.dark` on <html> to toggle dark mode
    content: [
      './src/**/*.{js,ts,jsx,tsx}',
      './app/**/*.{js,ts,jsx,tsx}',
    ],
    theme: {
      extend: {},              // add customizations here if you like
    },
    plugins: [],
  };
  