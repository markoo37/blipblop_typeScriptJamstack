import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Egyéb beállítások...
  images: {
    domains: [
      // ide írd be a Supabase tárolód hostnevét
      "emloutunogkjjvoravpv.supabase.co",
    ],
  },
};

export default nextConfig;
